<?xml version="1.0" encoding="UTF-8"?>
<web-app version="6.0" 
         xmlns="https://jakarta.ee/xml/ns/jakartaee" 
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd">
    
    <display-name>BookStore</display-name>
    
    <!-- 1. Cấu hình trang mặc định -->
    <welcome-file-list>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>
    
    <!-- 2. Cấu hình Session Timeout (30 phút) -->
    <session-config>
        <session-timeout>30</session-timeout>
        <!-- Các cấu hình cookie session bảo mật (Optional) -->
        <cookie-config>
            <http-only>true</http-only>
            <!-- <secure>true</secure> --> <!-- Bật cái này nếu chạy trên HTTPS -->
        </cookie-config>
    </session-config>
    
    <!-- 3. Cấu hình trang lỗi tùy chỉnh -->
    <!-- Lỗi 404: Không tìm thấy trang -->
    <error-page>
        <error-code>404</error-code>
        <location>/error.jsp</location>
    </error-page>
    
    <!-- Lỗi 500: Lỗi Server nội bộ (Java Exception) -->
    <error-page>
        <error-code>500</error-code>
        <location>/error.jsp</location>
    </error-page>
    
    <!-- Lỗi 403: Không có quyền truy cập -->
    <error-page>
        <error-code>403</error-code>
        <location>/error.jsp</location>
    </error-page>

    <!-- 4. (Optional) Cấu hình MIME type cho các file tĩnh nếu cần thiết -->
    <mime-mapping>
        <extension>json</extension>
        <mime-type>application/json</mime-type>
    </mime-mapping>

</web-app>